# ejs-s

ejs-s

# build

1. make a `build` directory

   ```bash
       mkdir build && cd build
   ```

2. use cmake
   ```bash
       cmake .. && cmake --build . --config release --parallel 2 --clean-first
   ```
